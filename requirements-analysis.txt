# Dependencies for the offline analysis job (api/predict.py) ONLY.
#
# Kept out of api/requirements.txt on purpose: the web process never imports
# these. /api/analysis reads precomputed rows from player_predictions /
# matchup_predictions, and the scheduler runs predict.py as a SEPARATE process,
# so gunicorn stays light and a model fit can never block a request.
#
# deploy.sh installs these on the VM. Without them `python -m api.predict`
# fails with ModuleNotFoundError - which is why the Analysis page had never
# produced a single row in production.
-r api/requirements.txt
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.13,<2
scikit-learn>=1.5,<2
