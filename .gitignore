# Environment (contains secrets - never commit)
.env.local
.env.development
.env.production

# Databases
*.db

# Python
__pycache__/
.venv/
.pytest_cache/
*.pyc

# System / tooling
.DS_Store
node_modules/
.vercel/
dist/
build/

# Runtime logs. predict.log is appended by the predict job's subprocess
# (see _launch_predict) and rotated on the VM by deploy.sh's logrotate rule.
*.log
